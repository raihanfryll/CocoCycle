<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CocoCycle"
        tools:targetApi="31">

        <!-- Halaman Mulai jadi tampilan awal -->
        <activity
            android:name=".MulaiActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MasukActivity" />
        <activity android:name=".DaftarActivity"/>
        <activity android:name=".DashboardActivity"/>
        <activity android:name=".EcoMarketplaceActivity"/>
        <activity android:name=".TambahBatokKelapaActivity"/>
        <activity android:name=".DaftarBatokActivity"/>
        <activity android:name=".EditBatokKelapaActivity" />
        <activity android:name=".LokasiPengambilanActivity"/>
        <activity android:name=".DetailTransaksiActivity"/>
       








    </application>

</manifest>
